//TODO
void checkCO2()
{
	//TODO
	/*int ph = aquaGodState.getAquariumPH();

	if (ph < settings.aquariumPH)
	{
		if (deviceGetState(CO2))
		{
			relayOff(CO2);
			Log.Info(F("PH Too Low. Stop CO2"CR));
		}
	}
	else 
		if (!deviceGetState(CO2) && getDeviceStateByTime(settings.co2OnTime, settings.co2OffTime))
			relayOn(CO2);*/
}












